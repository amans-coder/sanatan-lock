# SadhanaLock — Deferred Work

> Generated by /plan-eng-review + /plan-ceo-review on 2026-05-28

---

## P0 — Must resolve before implementation starts

### ~~TODO: Resolve all `[DECIDE]` items in docs/BLOCKING_LOGIC.md~~
- **RESOLVED on 2026-05-28.** All 8 decisions made. See `docs/BLOCKING_LOGIC.md` §12.
- Night lock: configurable, device local tz, default 10PM-6AM
- Cooldown: configurable 5/15/30/60 min, default 15
- Gentle: instant Om. Strict: 10s wait then Om
- Morning gate: must open app and pray (overlay doesn't count)
- Clock protection: not required (trust user)
- Denylist: not overridable
- Icons: paginated (getAppIcon per app)
- UsageStats: battery-friendly polling (~1500-2000ms)

### ~~TODO: Rename app identity (app.json, package name)~~
- **Why:** `app.json` says "frontend" — must be "SadhanaLock" / "sadhanalock" before first
  EAS build. Changing slug after first build breaks OTA updates permanently.
- **Effort:** S
- **RESOLVED on 2026-05-29.** Set to SadhanaLock/sadhanalock/com.sadhanalock.app.

### TODO: Generate Play Store signing key
- **Why:** Plan defers to Week 5. Losing a signing key = can never update the app on Play Store.
  Must generate early and back up securely. Also needed for any EAS build.
- **Effort:** S
- **Blocked by:** Nothing — do this Week 1

### TODO: Remove AsyncStorage dependency
- **Why:** `package.json` includes `@react-native-async-storage/async-storage` and the
  storage wrapper imports it. Plan §6 says "DO NOT install AsyncStorage" and use MMKV.
  Current code contradicts the plan. Either switch to MMKV or accept AsyncStorage.
- **Effort:** S (if accepting AsyncStorage) / M (if migrating to MMKV)
- **RESOLVED:** Keep AsyncStorage — it is already wired and works. Removed the "DO NOT use AsyncStorage" rule from PLAN.md §6.

### TODO: Source all content (Gita, mantras, Chalisas, Stotrams)
- **Why:** Content is critical-path for both Track A (scripture reader) and Track B (overlay).
  700 Gita verses + mantras for 7 deities + Chalisas + Stotrams + Aartis + festival dates.
  All need Devanagari + transliteration + English meaning.
- **Effort:** L (content sourcing, not code)
- **Blocked by:** Pandit reviewer identification (see below)

### ~~TODO: Identify pandit/Sanskrit expert reviewer~~
- **RESOLVED: Not required for V1.** Content accuracy handled by team internally.

---

## P1 — Must complete during implementation

### TODO: Set up CI workflows
- **Why:** Plan references `.github/workflows/expo-ci.yml` and `android-ci.yml` but neither
  exists. Without CI, PRs merge without any automated checks.
- **Effort:** M
- **Blocked by:** First real code landing (need something to build/test)

### TODO: Set up EAS Build configuration
- **Why:** No `eas.json`, no build profiles, no EAS project linked. Required for any
  Android APK build beyond Expo Go.
- **Effort:** S
- **Blocked by:** app.json rename (P0 TODO above)

### TODO: Build color token sync script
- **Why:** Color tokens exist in `colors.ts` (TS) AND `colors.xml` (Kotlin). Manual sync
  will drift. Need a script that generates one from the other at build time.
- **Effort:** S
- **Blocked by:** Both files existing (neither created yet)

### TODO: Backend auth + input validation
- **Why:** Backend currently has CORS `allow_origins=["*"]` and zero auth. Any client can
  read/write to MongoDB. `client_name` goes to DB without sanitization.
- **Auth:** Google Sign-In via Emergent built-in auth. Track A (Emergent) owns the auth flow.
  Backend validates auth tokens from Emergent.
- **Effort:** M
- **Blocked by:** Emergent auth integration (Track A)

---

## P2 — Post-V1 / V1.1

### TODO: Breathe mode (One Sec-style 5-second pause)
- **Why:** Research shows even 3-second friction reduces mindless app opens by 57%.
  Would widen appeal beyond devout users. Skipped in CEO review — dilutes spiritual identity.
- **Effort:** S
- **Blocked by:** V1 launch

### TODO: Home screen widget (verse + streak counter)
- **Why:** High-visibility engagement feature. Skipped in CEO review — not core to blocking.
- **Effort:** M (AppWidgetProvider + RemoteViews in Kotlin)
- **Blocked by:** V1 launch

### TODO: iOS support (Screen Time API)
- **Why:** Explicitly deferred to V2. Different platform, different API, different App Store
  review process. Expo Module architecture supports it — no path dependency.
- **Effort:** XL
- **Blocked by:** V1 Android launch + validation

### TODO: Dark mode
- **Why:** Explicitly deferred in plan. Deity color themes need dark variants.
- **Effort:** M
- **Blocked by:** V1 launch + design system finalized

### TODO: Audio recitations
- **Why:** Deferred in plan. Would add audio playback during prayer overlay.
- **Effort:** M
- **Blocked by:** V1 launch
